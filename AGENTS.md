# HoneyChain
Use the approved user specification and corrections. Keep FastAPI as the sole telemetry gateway. Enforce ownership, jurisdiction, device authentication and lab-role separation. No invented AI claims or trust scores. Label synthetic data. SHA-256 is a hash, not a signature. Run relevant tests after changes. Never commit secrets, generated credentials or databases.
